package slider.desktop;

import slider.core.Slider;
import slider.core.view.SliderObserver;

public class SwingSliderPresenter extends SliderObserver {
    
    public SwingSliderPresenter(Slider slider) {
        super(slider);
    }

}
